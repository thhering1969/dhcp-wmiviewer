*** Begin Patch
*** Update File: DhcpWmiViewer.csproj
@@
-</Project>
+</Project>
+  <!-- Temporärer Ausschluss: alle diagnostics_*-Ordner vom Kompilieren ausschließen.
+       Verhindert 'type already defined'-Fehler wenn diagnostic-Snippets/Backups
+       versehentlich im Projektordner liegen. Entferne diese ItemGroup wieder,
+       sobald Du die diagnostics-Ordner extern archiviert hast. -->
+  <ItemGroup>
+    <!-- Entfernt alle .cs Dateien unter diagnostics_*/ vom Build -->
+    <Compile Remove="diagnostics_*/**/*.cs" />
+  </ItemGroup>
+</Project>
*** End Patch
